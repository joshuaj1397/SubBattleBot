module.exports = {
    name: 'view',
    description: 'View a SubBattle',
    async execute(message, args, monogoClient) {
        // Check if there is a view already
            // If there is no view, make one and pin it
            // If there is a view
                // Check if there are changes to the SubBattle
                    // If no changes link to the view
                    // If there are changes, edit it
    }
}